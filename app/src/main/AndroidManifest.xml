<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ryan.unofficialhendrixapp"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="21" />

    <application
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:icon="@drawable/ic_launcher"
        android:logo="@drawable/ic_launcher"
        android:allowBackup="true"
        android:hardwareAccelerated="false" >

        <activity
            android:name=".activities.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data android:name="android.app.default_searchable"
                android:value=".SearchableActivity" >
            </meta-data>
        </activity>
        <activity
            android:name=".activities.StaffDetailActivity"
            android:parentActivityName=".activities.MainActivity" >
        </activity>
        <activity
            android:name=".activities.SearchableActivity"
            android:label="@string/title_activity_searchable"
            android:parentActivityName=".activities.MainActivity" >

            <meta-data android:name="android.app.searchable"
                android:resource="@xml/staff_search">
            </meta-data>

            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
        </activity>

        <service
            android:name=".services.NewsRefreshService"
            android:exported="false">
        </service>
        <service
            android:name=".services.StaffDatabaseService"
            android:exported="false">
        </service>

        <provider
            android:name=".data.HendrixProvider"
            android:authorities="com.ryan.unofficialhendrixapp.provider"
            android:exported="false" >
        </provider>
    </application>
</manifest>
